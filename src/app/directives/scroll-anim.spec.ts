import { ScrollAnim } from './scroll-anim';

describe('ScrollAnim', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnim();
    expect(directive).toBeTruthy();
  });
});
