import { TypewriterDirective } from './typewriter';

describe('TypewriterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypewriterDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
